<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Delete User</title>
  <link rel="stylesheet" href="/delete.css">
</head>
<body>
  <div class="container">
    <h1 class="heading">⚠️ Delete User Account</h1>
    <p class="desc">You’re about to erase this identity from the digital world. Confirm by entering password.</p>

    <form action="/users/<%= userdata.id %>?_method=DELETE" method="POST">
      <div class="field">
        <label for="username">Username</label>
        <input type="text" value="<%= userdata.username %>" readonly>
      </div>

      <div class="field">
        <label for="content">Content</label>
        <textarea readonly><%= userdata.content %></textarea>
      </div>

      <div class="field">
        <label for="password">Confirm Password</label>
        <input type="password" name="password" placeholder="Enter your password" required>
      </div>

      <button class="btn delete" type="submit">💀 Delete Permanently</button>
      <a href="/users" class="btn back">⬅️ Cancel</a>
    </form>
  </div>
</body>
</html>
